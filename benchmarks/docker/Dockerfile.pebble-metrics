FROM golang:1.25-alpine AS builder

WORKDIR /build

RUN apk add --no-cache gcc musl-dev

COPY go.mod go.sum ./
RUN go mod download

COPY . .

RUN CGO_ENABLED=1 go build -o pebble-runner ./cmd/pebble-runner-metrics

FROM alpine:latest

RUN apk add --no-cache ca-certificates

WORKDIR /app

COPY --from=builder /build/pebble-runner .

RUN mkdir -p /data/pebble

EXPOSE 9090

CMD ["./pebble-runner"]
